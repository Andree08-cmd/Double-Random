> [!IMPORTANT]
> **Status:** The code for this step is not ready yet because the first step is still under development.
>
> **How it works:** This script will use the same core logic as the first one, but with `MultiPV = 5` (exclusively for positions that pass the initial filter). 
> - The original `MultiPV = 2` will be maintained.
> - The other 3 engine suggestions will use a score width threshold of `+/- 0.8`.
